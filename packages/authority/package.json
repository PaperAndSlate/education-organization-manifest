{
  "name": "@paperandslate/eom-authority",
  "version": "0.1.0",
  "description": "Offline EOM origin and delegated publication-authority evaluation.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@paperandslate/eom-core": "workspace:*",
    "@paperandslate/eom-validator": "workspace:*"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --outDir dist --rootDir src --declaration --declarationMap",
    "typecheck": "tsc --noEmit",
    "lint": "true"
  }
}
