# @paperandslate/eom-schema

Bundled, versioned JSON Schema 2020-12 artifacts for EOM. The canonical source files live in the repository `schemas/1.0/` tree; this package exposes the offline catalog to the reference validator.
