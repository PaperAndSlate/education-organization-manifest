# Controlled source inventory

`source-index.json` is a synthetic, review-only inventory example. A source record identifies the approved locator, source authority, retrieval time, rights status, digest, likely modules, and review owner. Raw documents and snapshots do not belong in public EOM output; retain only what the operator is permitted to retain.
