# Module fixtures

`catalog-fixtures.json` contains one deterministic public resource document for every module schema in the EOM 1.0 registry. The module test suite extracts each entry and validates it against its type-selected bundled schema.

These are fictional `.example` resources. Staff data is deliberately public with review metadata; statistics are aggregate observations; meals carry an allergen disclaimer; and no student-level or private transport data is included.
