# Proposed well-known URI registration submission

## Status

Prepared draft; not submitted and not accepted. The selected suffix remains a working proposal.

## Draft fields

- URI suffix: `educational-organization-manifest`
- Specification: `https://paperandslate.org/spec/eom/1.0`
- Change controller: the accountable paper&slate legal/foundation entity identified at filing time
- Expected representation: UTF-8 JSON
- Discovery URL: `https://<education-origin>/.well-known/educational-organization-manifest`
- Initial media type: `application/json`; a custom type is deferred
- Related links: existing `describedby`, `canonical`, and `profile` relations

## Required filing evidence

1. Recheck the well-known registry and record the date, source, and collision analysis.
2. Freeze the candidate specification/schema URLs and attach the checksum manifest.
3. Attach the security, privacy, redirect, caching, and size behavior sections.
4. Identify the final change controller and correction contact.
5. Record the registry decision verbatim with its date; until then retain `blocked-external` status.

The repository’s local test suite cannot perform or attest to the registry action.
