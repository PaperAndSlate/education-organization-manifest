# Fictional River District example

This capture demonstrates a district manifest with two public organizations and no student-level
data. Every origin is reserved under `.example`; it is a fixture, not a live district publication.
