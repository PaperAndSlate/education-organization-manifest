# Fictional signed-school example

This capture demonstrates the optional EOM Ed25519 detached-signature profile with a public key
set, a signed organization resource, and a root `signing` declaration. The vector is synthetic and
uses the reserved `ecme-high.example` origin; verification is integrity evidence, not factual or
institutional authorization.
